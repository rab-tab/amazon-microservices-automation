shard.count=4
shard.0.url=jdbc:postgresql://order-db-shard0:5432/orders
shard.0.username=${SHARD0_USER}
shard.0.password=${SHARD0_PASS}
shard.1.url=jdbc:postgresql://order-db-shard1:5432/orders
shard.1.username=${SHARD1_USER}
shard.1.password=${SHARD1_PASS}
shard.2.url=jdbc:postgresql://order-db-shard2:5432/orders
shard.2.username=${SHARD2_USER}
shard.2.password=${SHARD2_PASS}
shard.3.url=jdbc:postgresql://order-db-shard3:5432/orders
shard.3.username=${SHARD3_USER}
shard.3.password=${SHARD3_PASS}